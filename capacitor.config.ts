import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-tabs-custom',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
